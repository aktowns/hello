(define woo (:: (-> string string)) (str.concat "woo "))
(term.println (woo "waa"))
(term.println (car [1 2 3]))
(term.println (cdr [1 2 3]))
(term.println (cons "a" [1 2 3]))

(term.println (str.substr "Hello" 0 4))

(defun hello (x) (term.println (str.concat "hello " x)))

(hello "world")
